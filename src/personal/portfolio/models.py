from django.db import models

# Create your models here.
# class australianaModel(models.Model):
#     mainText = models.TextField(blank=False, null=False)
#     created_at = models.DateTimeField(auto_now_add=True, blank=True, null=True)
#     questionText = models.TextField(blank=False, null=True, default="Question")